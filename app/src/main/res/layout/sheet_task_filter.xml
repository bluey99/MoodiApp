<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="24dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:text="@string/task_filter_title"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#085F63"
        android:layout_marginBottom="16dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <LinearLayout
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp">

        <ImageView
            android:id="@+id/btnMom"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:layout_marginEnd="24dp"
            android:src="@drawable/ic_parent"
            android:contentDescription="@string/task_filter_by_parent"
            android:background="@drawable/pill_button_selector"
            android:padding="12dp" />

        <ImageView
            android:id="@+id/btnTherapist"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:src="@drawable/ic_therapist"
            android:contentDescription="@string/task_filter_by_therapist"
            android:background="@drawable/pill_button_selector"
            android:padding="12dp" />

    </LinearLayout>

    <Button
        android:id="@+id/btnAll"
        android:text="@string/task_filter_clear"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

</LinearLayout>