<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#E3F4F4"
    android:fitsSystemWindows="true">

    <!-- Larger Back Button -->
    <ImageView
        android:id="@+id/btnBack"
        android:layout_width="94dp"
        android:layout_height="78dp"
        android:layout_marginTop="20dp"
        android:padding="5dp"
        android:src="@drawable/ic_arrow_back"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Larger Header Title -->
    <TextView
        android:id="@+id/txtHeaderTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Log My Feelings"
        android:textStyle="bold"
        android:textSize="26sp"
        android:textColor="#000000"
        android:layout_marginStart="14dp"
        app:layout_constraintStart_toEndOf="@id/btnBack"
        app:layout_constraintTop_toTopOf="@id/btnBack"
        app:layout_constraintBottom_toBottomOf="@id/btnBack" />

    <!-- Larger & Thicker Progress Bar Background -->
    <View
        android:id="@+id/stepProgressBar"
        android:layout_width="0dp"
        android:layout_height="10dp"
        android:background="#C3D0D9"
        android:layout_marginTop="22dp"
        app:layout_constraintTop_toBottomOf="@id/btnBack"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Larger Purple Progress Fill -->
    <View
        android:id="@+id/stepProgressFill"
        android:layout_width="0dp"
        android:layout_height="10dp"
        android:background="#7768AE"
        app:layout_constraintTop_toTopOf="@id/stepProgressBar"
        app:layout_constraintBottom_toBottomOf="@id/stepProgressBar"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Step Indicator Text -->
    <TextView
        android:id="@+id/txtStepIndicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Step 1 of 7"
        android:textSize="16sp"
        android:textColor="#51787C"
        android:layout_marginTop="14dp"
        app:layout_constraintTop_toBottomOf="@id/stepProgressBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Fragment Container -->
    <FrameLayout
        android:id="@+id/fragmentContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@id/txtStepIndicator"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
