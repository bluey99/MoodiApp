<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ASDProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ASDProject" parent="Base.Theme.ASDProject" />
    <style name="NoButtonMaterialTint" parent="@android:style/Widget.Button">
        <item name="android:backgroundTint">@null</item>
        <item name="android:foregroundTint">@null</item>
        <item name="android:stateListAnimator">@null</item> <!-- Prevent weird elevation animation -->
    </style>

</resources>